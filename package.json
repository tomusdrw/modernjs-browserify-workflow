{
  "name": "project1",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "watchify src/main.js -o src/bundle.js && cd src live-server & ",
    "build": "browserify src/main.js -o src/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "13.0.x",
    "live-server": "^1.0.0",
    "watchify": "^3.7.0"
  }
}
